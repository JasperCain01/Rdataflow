% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_isolated.R
\name{parse_one_select_isolated}
\alias{parse_one_select_isolated}
\title{Parse one SELECT-bearing statement in a subprocess}
\usage{
parse_one_select_isolated(
  sql,
  schema = NULL,
  dialect = "tsql",
  skipped_log = character(),
  timeout_ms = 30000L
)
}
\arguments{
\item{sql}{A length-1 character vector: the pre-processed SQL for one
SELECT-bearing statement (variables substituted, schema merged).}

\item{schema}{An optional \code{rdataflow_schema} object (converted to a plain
list before being passed to the child).}

\item{dialect}{sqlglot dialect name; default \code{"tsql"}.}

\item{skipped_log}{A character vector of previously skipped-statement
messages; the function may append to it.}

\item{timeout_ms}{Integer milliseconds to wait before giving up on the child.
Default 30 000 (30 s).}
}
\value{
A list with two elements:
\itemize{
\item \code{result}: the nested list returned by sqlglot for this statement, or
\code{NULL} on failure.
\item \code{skipped_log}: character vector (the input log, possibly extended).
}
}
\description{
Sends \code{sql} to a persistent \link[callr:r_session]{callr::r_session} child where reticulate and
sqlglot perform the extraction. If the child crashes or times out, returns
\code{NULL} and the error is appended to the \code{skipped_log} in the returned list.
The parent R session is always preserved.
}
