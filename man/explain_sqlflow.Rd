% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{explain_sqlflow}
\alias{explain_sqlflow}
\title{Explain a SQL script's data flow as text}
\usage{
explain_sqlflow(
  x,
  schema = NULL,
  dialect = "tsql",
  format = c("text", "markdown")
)
}
\arguments{
\item{x}{A SQL script as a length-1 character string, or an
\code{rdataflow_ir} object.}

\item{schema, dialect}{Passed to \code{\link[=parse_sql]{parse_sql()}} when \code{x} is a SQL string;
ignored otherwise.}

\item{format}{\code{"text"} (default) for indented plain text, or
\code{"markdown"} for a bulleted markdown document.}
}
\value{
A character vector of lines, classed \code{rdataflow_explanation} so
that printing renders it verbatim. Use \code{cat(..., sep = "\\n")} or
\code{writeLines()} to embed it elsewhere.
}
\description{
Produces a human-readable narrative of the lineage model: per statement
and stage, what is read, joined (with keys), grouped, filtered, and
computed. Accepts either a SQL string (the full parse pipeline is run) or
an \code{\link[=build_ir]{build_ir()}} result, classified or not.
}
\examples{
\dontrun{
explain_sqlflow("SELECT customer_id, SUM(amount) AS total
                 FROM dbo.orders GROUP BY customer_id")
}
}
