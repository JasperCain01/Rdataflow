% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sqlflow.R
\name{save_sqlflow}
\alias{save_sqlflow}
\title{Save a SQL dataflow diagram to a file}
\usage{
save_sqlflow(
  graph,
  file,
  show_col_edges = TRUE,
  show_legend = TRUE,
  rank_lanes = TRUE,
  rankdir = c("LR", "TB"),
  cluster_statements = TRUE
)
}
\arguments{
\item{graph}{An \code{rdataflow_graph} produced by \code{\link[=build_graph]{build_graph()}}.}

\item{file}{Output path; extension selects the format
(\code{.dot}, \code{.gv}, \code{.svg}, \code{.png}, \code{.pdf}).}

\item{show_col_edges}{If \code{TRUE} (default), draw column-level port-to-port
edges showing exact column lineage. If \code{FALSE}, draw structural
table→stage edges only, labelled with join types.}

\item{show_legend}{If \code{TRUE} (default), append a colour-coding legend
cluster to the diagram explaining node header colours, column role
colours, transformation type colours, and edge styles.}

\item{rank_lanes}{If \code{TRUE} (default), insert \code{rank=same} constraints so
that nodes at the same dependency depth are aligned in the same column.
This turns parallel branches into aligned vertical lanes, making complex
multi-stage scripts easier to follow. Pass \code{FALSE} to let Graphviz place
nodes freely.}

\item{rankdir}{Graphviz layout direction: \code{"LR"} (default, left-to-right)
or \code{"TB"} (top-to-bottom; useful for tall narrow display areas).}

\item{cluster_statements}{If \code{TRUE} (default), scripts with more than one
statement draw each statement's stages inside a labelled cluster box
("Statement 2 -> #summary"), making multi-statement flows much easier
to read. Single-statement scripts are never clustered.}
}
\value{
Invisibly, \code{file}.
}
\description{
Renders the graph and writes it to \code{file}. The format is chosen by file
extension: \code{.dot} / \code{.gv} write the raw Graphviz DOT source (no extra
dependencies); \code{.svg}, \code{.png}, and \code{.pdf} render via the \code{DiagrammeRsvg}
and \code{rsvg} packages (install both for image export).
}
