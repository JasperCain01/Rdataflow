% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_statements.R
\name{split_statements}
\alias{split_statements}
\title{Split a T-SQL script into individual statements}
\usage{
split_statements(sql)
}
\arguments{
\item{sql}{A length-1 character vector containing the full SQL script.}
}
\value{
A tibble with columns:
\itemize{
\item \code{seq} (integer): 1-based statement index in script order.
\item \code{text} (character): raw statement text, trimmed of leading/trailing
whitespace, with the terminator stripped.
\item \code{terminator} (character): \code{";"}, \code{"GO"}, or \code{""} (final statement
with no trailing terminator).
}

Empty chunks (e.g. between \verb{;;}) are silently dropped.
}
\description{
Applies a character-level state machine that recognises \verb{;} and lone \code{GO}
batch terminators only when they appear outside string literals, block
comments (including nested \verb{/* /* */ */} comments), line comments, and
bracket-quoted identifiers. \code{GO} is only treated as a terminator when it
is the first token on its line (matching SQL Server's rule that \code{GO} must
appear alone on a line); an optional repeat count (\verb{GO 5}) is consumed
with it. Original text (including comments) is preserved in each chunk.
}
\details{
Typographic ("smart") punctuation -- the kind Word, Outlook, and OneNote
substitute when SQL passes through them -- is normalised outside
comments: curly single quotes (U+2018/U+2019) open/close a string
literal and are emitted as \verb{'}, curly double quotes (U+201C/U+201D) as
\verb{"}, and non-breaking spaces (U+00A0) become plain spaces. A straight
\verb{'} \emph{inside} a curly-quoted literal is escaped to \code{''} so the resulting
literal stays valid; curly quotes inside a straight-quoted literal are
left untouched (they are content). Without this, a curly-quoted literal
reaches the SQL parser as garbage and a \verb{;} inside one would split the
statement in two.
}
