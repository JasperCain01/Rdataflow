% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temp_schema.R
\name{register_temp_table}
\alias{register_temp_table}
\title{Register a temp table's columns in the registry}
\usage{
register_temp_table(registry, table_name, columns_tbl, origin_seq)
}
\arguments{
\item{registry}{A tibble as returned by \code{\link[=new_temp_registry]{new_temp_registry()}}.}

\item{table_name}{Name of the temp table (e.g. \code{"#prison_terms"}).}

\item{columns_tbl}{A tibble with at least \code{column} and \code{type} columns and
optionally \code{ordinal}, as returned by \code{parse_create_table_columns()}.}

\item{origin_seq}{Integer: the \code{seq} index of the originating statement.}
}
\value{
The updated registry tibble.
}
\description{
Appends column metadata for \code{table_name} to \code{registry}. If columns for that
table are already present, the new rows are appended after the existing ones
(last-registration wins for downstream merges because \code{merge_temp_schema()}
picks the most recent set of columns).
}
