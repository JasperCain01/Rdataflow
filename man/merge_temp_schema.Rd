% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temp_schema.R
\name{merge_temp_schema}
\alias{merge_temp_schema}
\title{Merge temp-table columns into a sqlglot schema object}
\usage{
merge_temp_schema(sql, schema, temp_registry)
}
\arguments{
\item{sql}{The SQL text of the statement about to be parsed.}

\item{schema}{An \code{rdataflow_schema} object (may be \code{NULL}).}

\item{temp_registry}{A tibble as returned by \code{\link[=new_temp_registry]{new_temp_registry()}}.}
}
\value{
A merged \code{rdataflow_schema} object, or \code{NULL} if both inputs are
empty.
}
\description{
Combines columns from \code{temp_registry} that match tables referenced in \code{sql}
with an existing rdataflow_schema, returning a new schema that includes both.
This allows the sqlglot qualifier to resolve temp-table column references
alongside regular catalog columns.
}
\details{
Only the \emph{most recently registered} set of columns for each temp table is
used (the highest \code{origin_seq}), so a later CREATE TABLE or SELECT INTO
supersedes an earlier one.
}
