% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_setup.R
\name{check_setup}
\alias{check_setup}
\title{Diagnose the Python / sqlglot setup}
\usage{
check_setup()
}
\value{
Invisibly, a named logical vector, one element per check.
}
\description{
Checks each link in the parsing chain in dependency order and prints a
\code{ok} / \code{FAILED} line per step: is \code{reticulate} installed, which Python it
found, is \code{sqlglot} importable (and its version), can the bundled
\code{rdataflow_sqlglot.py} module be located, does the isolated \code{callr}
child session parse a trivial statement, and does a tiny script survive
the full \code{\link[=parse_sql]{parse_sql()}} pipeline.
}
\details{
The first \code{FAILED} step is the broken link; later failures are usually
just its consequences. Typical fixes: \code{install.packages("reticulate")},
\code{\link[=install_sqlglot]{install_sqlglot()}}, reinstalling the package (or setting
\code{RDATAFLOW_PY_PATH}) when the bundled module is missing, and checking
\code{reticulate::py_config()} in a fresh session when only the child fails.
}
\examples{
\dontrun{
check_setup()
}
}
