% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/declare_vars.R
\name{substitute_vars}
\alias{substitute_vars}
\title{Substitute @variable references in a SQL string}
\usage{
substitute_vars(text, variables)
}
\arguments{
\item{text}{A length-1 character vector.}

\item{variables}{A tibble as returned by \code{\link[=extract_declare]{extract_declare()}} (multiple rows
accepted; processed in the order given).}
}
\value{
The text with \verb{@variable} references replaced.
}
\description{
Replaces each \verb{@variable} reference with its registered value, following
the policy:
\itemize{
\item \strong{Literal} values (plain string/number/date, optionally one CAST) →
substituted verbatim, preserving the exact filter predicate.
\item \strong{Non-literal} values (DATEADD, GETDATE, arithmetic, ...) → replaced
by a typed sentinel chosen by the variable's declared type. This removes
crash-inducing T-SQL expressions before they reach the Python tokeniser
while keeping the predicate shape intact.
\item Variables with unknown types and non-literal values → left unsubstituted
(callers should log these as "could not resolve").
}
}
\details{
Matching is word-boundary, \code{@}-anchored: \verb{@StartDate} will NOT match a
longer \verb{@StartDateTime}.
}
