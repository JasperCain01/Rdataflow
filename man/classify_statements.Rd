% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_statements.R
\name{classify_statements}
\alias{classify_statements}
\title{Classify raw split statements by SQL kind}
\usage{
classify_statements(statements_raw)
}
\arguments{
\item{statements_raw}{A tibble with at least \code{seq} and \code{text} columns,
as returned by \code{\link[=split_statements]{split_statements()}}.}
}
\value{
The input tibble with an additional \code{kind} character column. Values:
\code{"declare"}, \code{"set_var"}, \code{"drop"}, \code{"transaction"}, \code{"merge"},
\code{"update"}, \code{"create_index"}, \code{"create_table"}, \code{"select_into"},
\code{"select"}, \code{"insert_select"}, \code{"insert_values"}, \code{"unknown"}.
}
\description{
Adds a \code{kind} column to the tibble returned by \code{\link[=split_statements]{split_statements()}}.
Classification follows the ordered rule table in the package design
(DECLARE > SET > DROP > MERGE > UPDATE > CREATE TABLE > INDEX > WITH >
INSERT > SELECT), using a paren-depth 0, string-aware SELECT test to
discriminate CTAS/INSERT-SELECT from plain CREATE TABLE / INSERT-VALUES.
}
