% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{schema_from_list}
\alias{schema_from_list}
\title{Build a schema object from a named list}
\usage{
schema_from_list(tables)
}
\arguments{
\item{tables}{A named list describing each table's columns (see details).}
}
\value{
An object of class \code{rdataflow_schema}.
}
\description{
This is the offline / testing entry point: it lets you describe table
metadata inline without a live database connection. Each list element is a
table; its name is the (optionally qualified) table identifier and its
value lists the columns.
}
\details{
Column values may be supplied two ways:
\itemize{
\item an \strong{unnamed} character vector of column names (types unknown), or
\item a \strong{named} character vector mapping column name to SQL type.
}

Table identifiers may be qualified and bracket-quoted in T-SQL style, e.g.
\code{"dbo.customers"}, \code{"[dbo].[customers]"}, or \code{"sales.dbo.orders"}.
}
\examples{
schema_from_list(list(
  "dbo.customers" = c("customer_id", "name", "region_id"),
  "dbo.regions"   = c(region_id = "INT", region_name = "VARCHAR(50)")
))
}
