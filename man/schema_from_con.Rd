% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{schema_from_con}
\alias{schema_from_con}
\title{Build a schema object from a live database connection}
\usage{
schema_from_con(con, tables = NULL, catalog = NULL)
}
\arguments{
\item{con}{A \code{DBI} connection (e.g. created with \code{odbc::dbConnect()}).}

\item{tables}{Optional character vector of table identifiers to restrict
the catalog to (matched on table name, optionally \code{schema.table}). When
\code{NULL} (default) every table the connection can see is read.}

\item{catalog}{Optional database/catalog name to filter on.}
}
\value{
An object of class \code{rdataflow_schema}.
}
\description{
Reads column metadata from \code{INFORMATION_SCHEMA.COLUMNS} over a \code{DBI}
connection (typically an \code{odbc} connection to SQL Server). This is the
primary, zero-manual-effort metadata source for real scripts.
}
